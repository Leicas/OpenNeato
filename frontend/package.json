{
    "name": "neato-web",
    "private": true,
    "version": "0.0.1",
    "scripts": {
        "dev": "vite",
        "build": "biome check && tsc --noEmit && vite build && node scripts/embed_frontend.js",
        "preview": "vite preview",
        "check": "biome check",
        "fix": "biome check --write",
        "fix:unsafe": "biome check --write --unsafe"
    },
    "dependencies": {
        "preact": "10.29.0"
    },
    "devDependencies": {
        "@biomejs/biome": "2.4.7",
        "@preact/preset-vite": "2.10.3",
        "typescript": "5.9.3",
        "vite": "7.3.1"
    }
}
